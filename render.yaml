# render.yaml
services:
  - type: web
    name: Youtube-Trend-API
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --timeout 3600"